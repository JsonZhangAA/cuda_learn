#include <cutil.h>  
//extern "C" void VectAdd(int *a, int *b, int *c, int length);  
void VectAdd(int *a, int *b, int *c, int length)  
{     
    for(int i=0;i<5;i++){
        printf("this is CUDA! Success!!!");
    }
}  